name: Build and Test on Docker
on:
  push:
    branches: [ main, ci ]
  pull_request:
    branches: [ main ]
  
  workflow_dispatch:

jobs:
  name: 'Test'
  description: 'Test on Docker'
  runs:
    using: 'docker'
    image: ubuntu:20.04
    entrypoint: ".github/actions/test/build_and_test.sh"
